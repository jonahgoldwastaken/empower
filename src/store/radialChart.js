import { readable, writable } from 'svelte/store'

export const margin = readable(40)
export const arcMinRadius = readable(10)
export const arcPadding = readable(20)
export const labelPadding = readable(-10)
